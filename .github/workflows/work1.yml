name: CI1

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Get OS
      run: echo OS is %OS%
    - name: Get PATH
      run: echo %PATH%
    - name: Run a multi-line script
      run: |
        echo building
        MSBuild.exe -version
        echo testing
        echo deploying
